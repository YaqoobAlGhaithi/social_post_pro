import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yaqoob.socialpostpro',
  appName: 'Social Post Pro',
  webDir: 'dist'
};

export default config;
